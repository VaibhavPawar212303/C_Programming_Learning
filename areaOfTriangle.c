#include <stdio.h>
int main()
{
    int base, height;
    float area;
    printf("Enter Your Base And Height: ");
    scanf("%d", "%d", &base, &height);
    area = base * height / 2;
    printf("Area Of Triangle: %0.2f", area);
}